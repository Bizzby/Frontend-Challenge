/*
 * bizzby.styl
 *
 * I have experience with other CSS preprocessors, but stylus is my favourite
 * because it:
 * - is syntactically similar to javascript
 * - is indentation based
 * - doesn't care about braces, colons, and semi-colons
 * - has transparent mixins
 * 
 * Gonna try out @fat's pretty sweet LESS coding guidelines here...
 * https://medium.com/@fat/mediums-css-is-actually-pretty-fucking-good-b8e2a6c78b06
 * https://gist.github.com/fat/a47b882eb5f84293c4ed#components

 * I've used BEM quite a bit previously, but really like the ideas in the
 * article — especially the approach towards components, which fits perfectly in
 * with React's idioms.
 *
 * Oridinarily I would split this file out into component styles, variables,
 * etc, but for the sake of speed I'm piling it all in here.
 */
@import "nib"
@import "jeet"
@import "rupture"

// nib reset
global-reset()


//// Colours
color-white = #f5f5f5
color-red = #F2594B
color-red--light = #EE6B5F
color-red--xlight = #F57A6F
color-red--dark = #D13F32
color-red--xdark = #9D2F25

//// Shadows
shadow--below = 0 3px 5px rgba(black, 0.1)
shadow--left = -3px 0 5px rgba(black, 0.1)

//// Font stacks
// Yeah the fat sans is very different from your font, but "GET PRICE AND
// BIZZBY IT" sounded like a movie title, so a movie-sans felt apt :D
font-heading = "sneakers-script-narrow", "sans-serif"
font-body = "freight-sans-pro", "sans-serif"

//// Easings
// Taken from http://easings.net/
easeOutBack = cubic-bezier(0.175, 0.885, 0.32, 1.275)
easeInOutBack = cubic-bezier(0.68, -0.55, 0.265, 1.55)
easeOutExpo = cubic-bezier(0.19, 1, 0.22, 1)
easeOutSine = cubic-bezier(0.39, 0.575, 0.565, 1)

//// Z-indexes
z-index--detailView = 100
z-index--page-below = 100
z-index--page-above = 200
z-index--headerBar =  300

//// Basics
html
  transition opacity 300ms
  opacity 0
  &.wf-inactive
  &.wf-active
    opacity 1

body
  color white
  background color-red
  font 16px/1.5 font-body
h1
  font-family font-heading
  text-align center
  text-transform uppercase
  font-size 2em
h2
  font-size 1.5em
  text-align center
a
  color white
  text-decoration none

//// Components
.bzIcon
  display inline-block
  width 1.5em
  height @width
  fill white
  vertical-align -11px
  overflow hidden
  padding 0.15em 0

.callOut
  margin 0 auto
  margin-bottom 1rem
  font-size 1rem
  text-align center
.callOut a
  display inline-block
  color color-red--xdark

.bzBtn
  appearance none
  display block
  background color-red--dark
  box-shadow 0 2px 0 color-red--xdark
  font-family font-heading
  text-align center
  text-transform uppercase
  border 0
  border-radius 3px
  font-size 1.2rem
  padding 0.75rem 2rem
  color white
  margin 0 auto 1rem

.page
  position absolute
  width 100%
  top 0
  left 0
  height 100%

.headerBar
  position relative
  z-index z-index--headerBar
  background color-red--light
  height 3em
  line-height 3em
  margin-bottom 1em
  border-bottom 1px solid color-red--dark
.headerBar-backButton
  position absolute
  top 0
  left 0
.headerBar-backButton a
  display block
  padding 0.5em
.headerBar-backButton .bzIcon
  position relative

.taskTypeList
  width 100%
  text-align center
  margin-bottom 0.5em
.taskTypeList-item
  display inline-block
  font-size 1.2em
.taskTypeList-item a
  display inline-block
  text-align center
  padding 0.25em 0.5em
  border 1px solid color-red--dark
  border-left-width 0px
  transition all 400ms easeOutExpo
.taskTypeList-item:first-child a
    border-radius 4px 0 0 4px
    border-left-width 1px
.taskTypeList-item:last-child a
    border-radius 0 4px 4px 0  
.taskTypeList-item .bzIcon
  fill color-red--dark
  display inline-block
  transition fill 400ms easeOutExpo
.taskTypeList-item.active
  a
    background color-red--dark
  .bzIcon
    fill white

.bzSelector
  display inline-block
  cursor pointer
  background color-red--xlight
  border-radius 5px
  padding 0 0.25em
  margin 0 0.25em
  line-height 1.4
.bzSelector:hover
  background color-white
  color color-red
  .bzIcon
    fill @color
.bzSelector .bzIcon
  font-size 70%

.detailView
  font-size 2.2em
  for num in 0..10
    .bzSelector:nth-child({num})
      animation-name scaleIn
      animation-duration 200ms
      animation-timing-function easeInOutBack
      animation-delay (num * 10 + 100)ms
.detailView p
  padding 0.5rem 0.75rem
  margin-bottom 0.5rem

//// Animations
@keyframes scaleIn
  from
    transform scale(1.4) translateY(-10%)
    opacity 0
  to
    transform scale(1) translateY(0)
    opacity 1