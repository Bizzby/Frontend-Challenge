/*
 * bizzby.styl
 *
 * I have experience with other CSS preprocessors, but stylus is my favourite
 * because it:
 * - is syntactically similar to javascript
 * - is indentation based
 * - doesn't care about braces, colons, and semi-colons
 * - has transparent mixins
 * 
 * Gonna try out @fat's pretty sweet LESS coding guidelines here...
 * https://medium.com/@fat/mediums-css-is-actually-pretty-fucking-good-b8e2a6c78b06
 * https://gist.github.com/fat/a47b882eb5f84293c4ed#components

 * I've used BEM quite a bit previously, but really like the ideas in the
 * article — especially the approach towards components, which fits perfectly in
 * with React's idioms.
 *
 * Oridinarily I would split this file out into component styles, variables,
 * etc, but for the sake of speed I'm piling it all in here.
 */
@import "nib"
@import "jeet"
@import "rupture"

// nib reset
global-reset()

// Colours
color-red = #F05B50
color-red--light = #F47B72
color-red--dark = #CF4038
color-red--xdark = #8F2924

// Font stacks
font-heading = "ff-cocon-web-pro", "sans-serif"
font-body = "freight-sans-pro", "sans-serif"

// Easings
// Taken from http://easings.net/
easeOutBack = cubic-bezier(0.175, 0.885, 0.32, 1.275)
easeInOutBack = cubic-bezier(0.68, -0.55, 0.265, 1.55)
easeOutExpo = cubic-bezier(0.19, 1, 0.22, 1)

// Basics
body
  color white
  background color-red
  font 1em/1.5 font-body
h1
  font-family font-heading
  text-shadow 0 2px 1px rgba(black, 0.2)
  text-align center
  text-transform uppercase
  font-size 2em
h2
  font-size 1.5em
  text-align center

// Components
.page
  position absolute
  width 100%
  top 0
  left 0

.headerBar
  background color-red--light
  box-shadow 0 3px 5px rgba(black, 0.1)
  height 3em
  line-height 3em
.headerBar-backButton
  position absolute
  top 0
  left 0
.headerBar-backButton a
  display block
  padding 0.5em
.headerBar-backButton .bzIcon
  position relative

.bzIcon
  display inline-block
  width 2em
  fill white
  vertical-align middle

.taskTypeList
  font-size 3em
  margin-top 1em
.taskTypeList-item
  display block
  text-align center
.taskTypeList-item .bzIcon
  fill color-red--dark
.taskTypeList-item a
  color white
  text-decoration none

// Animation etc.
.pageForward-enter
  transform translateX(100%)
  transition transform 500ms easeOutExpo
.pageForward-enter.pageForward-enter-active
  transform translateX(0)
.pageForward-leave
  transform translateX(0)
  transition transform 500ms easeOutExpo
.pageForward-leave.pageForward-leave-active
  transform translateX(-100%)

.pageBackward-enter
  transform translateX(-100%)
  transition transform 500ms easeOutExpo
.pageBackward-enter.pageBackward-enter-active
  transform translateX(0)
.pageBackward-leave
  transform translateX(0)
  transition transform 500ms easeOutExpo
.pageBackward-leave.pageBackward-leave-active
  transform translateX(100%)